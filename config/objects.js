const CONTAINER = 'container';
const MODEL = 'model';

const SCENE = {
    menuOfObjects: {
        type: CONTAINER,
        content: {
            buttonSource: {
                type: MODEL,
                model: Sprite,
                sprite: TT.stoneV,
            }
        },
    }
};